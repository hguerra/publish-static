begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_agro00" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."agro00") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_agro10" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."agro10") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_agro20" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."agro20") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_agro30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."agro30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_agro40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."agro40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_agro50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."agro50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_natveg00" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."natveg00") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_natveg10" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."natveg10") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_natveg20" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."natveg20") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_natveg30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."natveg30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_natveg40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."natveg40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_natveg50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."natveg50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_nonprodq00" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."nonprodq00") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_nonprodq10" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."nonprodq10") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_nonprodq20" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."nonprodq20") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_nonprodq30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."nonprodq30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_nonprodq40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."nonprodq40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_nonprodq50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."nonprodq50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_notrel00" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."notrel00") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_notrel10" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."notrel10") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_notrel20" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."notrel20") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_notrel30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."notrel30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_notrel40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."notrel40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_notrel50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."notrel50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_pltfor00" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."pltfor00") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_pltfor10" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."pltfor10") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_pltfor20" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."pltfor20") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_pltfor30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."pltfor30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_pltfor40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."pltfor40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_pltfor50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."pltfor50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_wetlnd00" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."wetlnd00") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_wetlnd10" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."wetlnd10") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_wetlnd20" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."wetlnd20") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_wetlnd30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."wetlnd30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_wetlnd40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."wetlnd40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_wetlnd50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."wetlnd50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;

begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_forreg30" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."forreg30") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_forreg40" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."forreg40") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
begin;update "output_brazilsforestcode_regions" set "fcnocra_dd_forreg50" = trunc(s_sum.s_total, 3) from (select s.region_id s_id, sum(s."forreg50") s_total from "brazilsforestcode_fcnocra_dd" s group by s.region_id order by s.region_id) s_sum where "output_brazilsforestcode_regions".id = s_sum.s_id;commit;
